{
  "name": "mis-pelis-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd api && npm install && npx prisma generate && cd ../client && npm install && npm run build"
  },
  "engines": {
    "node": ">=20.x"
  }
}
